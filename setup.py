from setuptools import setup

setup(
    name='flare',
    version='0.0.1',
    packages=['flare'],
    install_requires=['Flask', 'Flask-PyMongo'],
    url='https://github.com/re1/flare',
    license='',
    author='Markus Re1',
    author_email='markus@re1.at',
    description='A flask tutorial which eventually turned into a social news site'
)
